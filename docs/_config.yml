# build settings
markdown: GFM
remote_theme: just-the-docs/just-the-docs

# The diagrams on the Architecture page are ```mermaid code fences; just-the-docs only loads
# mermaid.js when this key is present.
mermaid:
  version: "10.9.1"
